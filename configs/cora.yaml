seed: 42

dataset:
  name: "Cora"

model:
  hidden_channels: 16
  dropout: 0.5

training:
  epochs: 200
  lr: 0.01
  weight_decay: 0.0005

sparsifier:
  name: "random"      # "random" or "degree"
  sparsity: 0.5       # fraction of edges to keep

